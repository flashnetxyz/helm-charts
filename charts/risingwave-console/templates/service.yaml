apiVersion: v1
kind: Service
metadata:
  name: {{ include "risingwave-console.fullname" . }}
  labels:
    {{- include "risingwave-console.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: webui
      port: {{ .Values.risingwave-console.port }}
    - name: metrics
      port: {{ .Values.metrics.port }}
    {{- if .Values.debug.enabled }}
    - name: debug
      port: {{ .Values.debug.port }}
    {{- end }}
  selector:
    {{- include "risingwave-console.selectorLabels" . | nindent 4 }}
