apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wavekit.fullname" . }}-test-connection"
  labels:
    {{- include "wavekit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wavekit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
